import { describe, it, expect } from 'vitest'

describe('Testing the functionality to determine if the test build is working:', () => {
  it('Should compare booleans', () => {
    expect(true).toBe(true)
  })
})